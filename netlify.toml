[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  # react-scripts 3.4.3 (webpack 4) needs --openssl-legacy-provider, which only
  # exists on Node 17+. Pin a modern Node so the flag in the build script is valid.
  NODE_VERSION = "20"
